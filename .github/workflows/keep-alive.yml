name: Keep Railway Service Alive

on:
  schedule:
    - cron: '*/9 * * * *'

jobs:
  ping-health:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: |
          curl -f https://pigeon-production.up.railway.app/healthz || exit 1
        continue-on-error: true 
